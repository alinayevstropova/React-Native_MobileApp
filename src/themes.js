import { StyleSheet } from 'react-native';

// Темная тема
export const darkTheme = StyleSheet.create({
  colors: {
    background: '#121212',       // Очень темный серый (почти черный) - основной фон
    text: '#00FF00',             // Неоново-зеленый / лайм - основной текст, активные элементы
    title: '#00FF00',            // Неоново-зеленый / лайм - заголовки
    buttonText: '#00FF00',       // Неоново-зеленый / лайм - текст на кнопках
    buttonBackground: 'rgba(0,0,0,0.7)', // Полупрозрачный черный - кнопки, карточки
    buttonBorderColor: '#00FF00', // Неоново-зеленый / лайм - границы кнопок
    neon: '#00FF00',             // Неоново-зеленый / лайм - акцентный цвет
    darkGray: '#333',            // Темный серый - второстепенные элементы
    bottomMenu: 'rgba(0,0,0,0.8)', // Полупрозрачный черный - нижнее меню
    gray: 'gray',                // Регулярный серый - менее важный текст
    gradientStart: 'rgba(0,0,0,0.8)', // Начало градиента - темный
    gradientEnd: 'rgba(0,0,0,0.6)',   // Конец градиента - немного светлее
    shadowColor: '#000',         // Черный - тени
    mapBackground: '#212121',    // Самый темный фон для карт
    mapRoad: '#00FFFF',          // Неоново-синий для дорог
    mapRoadStroke: '#FFFFFF',    // Белый для свечения дорог
    mapEaten: 'gray',            // Серый для съеденных дорог
  },
  textShadowColor: '#00FF00',      // Неоново-зеленый - для эффекта свечения
  textShadowOffset: { width: 0, height: 0 }, // Для эффекта свечения
  textShadowRadius: 10,           // Радиус свечения
  buttonBorderWidth: 2,           // Толщина границы кнопок
  mapStrokeWidth: 1.5,            // Толщина обводки дорог на карте
  // Дополнительные стили могут быть добавлены для специфических элементов, шрифтов и прочего
});

// Светлая тема
export const lightTheme = StyleSheet.create({
  colors: {
    background: '#FFFFFF',        // Белый или очень светлый серый - основной фон
    text: '#000000',              // Черный или очень темный серый - основной текст
    title: '#000000',             // Черный или очень темный серый - заголовки
    buttonText: '#000000',        // Черный или очень темный серый - текст на кнопках
    buttonBackground: '#DDDDDD',  // Светло-серый - кнопки, карточки
    buttonBorderColor: '#000000', // Черный или очень темный серый - границы кнопок
    neon: '#000000',              // Черный - акцентный цвет (можно изменить для светлой темы)
    darkGray: '#AAAAAA',          // Средний серый - второстепенные элементы
    bottomMenu: '#EEEEEE',        // Очень светлый серый - нижнее меню
    gray: '#888888',              // Регулярный серый - менее важный текст
    gradientStart: 'rgba(221, 221, 221, 0.8)', // Начало градиента - светлый
    gradientEnd: 'rgba(238, 238, 238, 0.6)',   // Конец градиента - немного темнее
    shadowColor: '#888888',       // Серый - тени
    mapBackground: '#F0F0F0',     // Самый светлый фон для карт
    mapRoad: '#000000',           // Черный или темный серый - для дорог в светлой теме
    mapRoadStroke: '#000000',     // Обводка дорог
    mapEaten: '#888888',          // Для съеденных дорог
  },
  textShadowColor: 'transparent',   // Без тени для текста в светлой теме
  textShadowOffset: { width: 0, height: 0 },
  textShadowRadius: 0,
  buttonBorderWidth: 1,            // Толщина границы кнопок
  mapStrokeWidth: 1.5,             // Толщина обводки дорог на карте
  // Дополнительные стили могут быть добавлены для специфических элементов, шрифтов и прочего
});
